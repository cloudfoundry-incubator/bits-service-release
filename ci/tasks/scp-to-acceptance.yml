---
platform: linux

image: docker:///flintstonecf/ci

inputs:
  - name: git-bits-service-release

params:
  SSH_CONNECTION_STRING:
  SSH_KEY:
  FILE:
  VERSION_FILE:
  RELEASE_NAME:

run:
  path: git-bits-service-release/ci/scripts/scp-to-acceptance.sh
