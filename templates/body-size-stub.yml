properties:
  bits-service:
    max_body_size: 2M
    app_stash:
      max_body_size: 5M
    buildpacks:
      max_body_size: 5M
    droplets:
      max_body_size: 5M
    packages:
      max_body_size: 5M
    buildpack_cache:
      max_body_size: 5M
