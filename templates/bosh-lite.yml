networks:
- name: default
  subnets:
  - cloud_properties:
      name: random
    range: (( metadata.networks.range ))
    reserved:
    - (( metadata.networks.reserved ))
    static:
    - (( metadata.networks.static ))

resource_pools:
  - name: common
    network: default
    stemcell:
      name: bosh-warden-boshlite-ubuntu-trusty-go_agent
      version: latest
    cloud_properties:
      instance_type: m1.small
      availability_zone:
      key_name:

metadata: (( merge ))
