networks:
- name: default
  subnets:
  - cloud_properties:
      name: random
    range: (( metadata.networks.range ))
    reserved:
    - (( metadata.networks.reserved ))

metadata:
  networks:
    reserved: 10.250.0.1
    range: 10.250.0.0./24
