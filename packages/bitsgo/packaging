set -ex

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.7)
export PATH=${GOROOT}/bin:${PATH}
export GOPATH=$PWD
export PATH=$GOPATH/bin:$PATH

mkdir src
mkdir bin
cp -a github.com src
cd src/github.com/petergtz/bitsgo/cmd/bitsgo
go install
cp $GOPATH/bin/bitsgo ${BOSH_INSTALL_TARGET}