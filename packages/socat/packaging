set -xe

tar xzf socat/socat-1.7.3.0.tar.gz
(
  set -xe
  cd socat-1.7.3.0
  LDFLAGS="-Wl,-rpath -Wl,${BOSH_INSTALL_TARGET}" ./configure --prefix=${BOSH_INSTALL_TARGET} --with-opt-dir=${BOSH_INSTALL_TARGET}
  make
  make install
)
